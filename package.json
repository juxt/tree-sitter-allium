{
  "name": "tree-sitter-allium",
  "version": "2.0.0",
  "description": "Tree-sitter grammar for the Allium language.",
  "license": "MIT",
  "main": "bindings/node",
  "files": [
    "grammar.js",
    "binding.gyp",
    "bindings/node/**",
    "queries/**",
    "src/**",
    "dist/**",
    "README.md",
    "package.json",
    "LICENSE"
  ],
  "scripts": {
    "generate": "tree-sitter generate",
    "build:native": "node-gyp rebuild && mkdir -p dist && cp build/Release/tree_sitter_allium_binding.node dist/tree-sitter-allium.node",
    "build:wasm": "tree-sitter build --wasm && mkdir -p dist && mv tree-sitter-allium.wasm dist/",
    "build": "npm run generate && npm run build:native && npm run build:wasm",
    "prepack": "npm run build",
    "lint": "eslint grammar.js bindings/",
    "test": "tree-sitter test"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.25.0"
  },
  "dependencies": {
    "node-addon-api": "^8.3.0",
    "node-gyp-build": "^4.8.4"
  }
}
